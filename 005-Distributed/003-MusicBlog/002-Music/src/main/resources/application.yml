server:
  port: 18080
  servlet:
    context-path: /music

application-name: music-80

spring:
  application:
    # 服务名
    name: music-service

eureka:
  client:
    # 注册中心的地址
    service-url.defaultZone: http://zpc:123456@localhost:18100/eureka_server/eureka
    # 是否在注册中心注册
    register-with-eureka: true
    # 是否从注册中心获取信息
    fetch-registry: false
